{
  "name": "donly-stylus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd donly-frontend && npm run dev",
    "build": "cd donly-frontend && npm run build",
    "start": "cd donly-frontend && npm run start",
    "lint": "cd donly-frontend && npm run lint",
    "install": "cd donly-frontend && npm install"
  },
  "workspaces": [
    "donly-frontend"
  ]
}
